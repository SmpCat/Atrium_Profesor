// EJEMPLO DE SINTAXIS DE LOS ELEMENTOS DE JAVASCRIPT

// Valores, operadores, expresiones, palabras clave y comentarios

//ambito global de variable
var variable_global;

//FUNCION DE JAVASCRIPT
//Elemento donde se agrupan las ordenas a ejecutar.
function nombre_funcion(parametro_a_recibir){

	// Variables en javascript
	//numericas 
	 var x= 34;
	 x=3.5;
	 //cadena
	 x="cadena de caracteres";
	 // booleanas
	 $variable_Boolena=true;
	 _variable_Boolena=false;
	 //sin valor
	 variable_sinvalor=null;
	 // valor indefinido
	 variable_indefinida=undefined;
	 //datos compuestos
	//tabla
	tabla_datos=["valor uno","valor dos","valor tres"];
	//objeto navegador 
	 objeto_Navegador=new Date();
	 //objeto en JSON
	 objeto_JSON={nombre_propiedad:valor_propiedad,nombre_propiedad2:valor_propiedad2};
	 
}

function nombre_funcion(){
	//ESTRUCTURAS DE PROGRAMACION
	//condicional if - alternativa simple
	if (condition) {
		
	}
	if (condition) {
		
	} else {

	}
	//condicional switch - alternativa compuesta
	switch (key) {
	case value:
		
		break;

	default:
		break;
	}
	//REPETITIVAS
	//repetitiva un numero fijo de veces
	for (var int = 0; int < VALOR_FINAL; int++) {
				
	}
	//repetitiva sobre una tabla 
	for (var int = 0; int < array.length; int++) {
		var array_element = array[int];
		
	}
	// repetitiva sobre una iteracion tabla/propiedades de un objeto
	for ( var iterable_element in iterable) {
		
	}
	//repetitiva hasta
	while (condition) {
		
	}
	//repetitiva mientras
	do {
		
	} while (condition);
	
	//OPERADORES DE CONDICION
	// ==		IGUALDAD
	// > 		MAYOR QUE
	// <		MENOR QUE
	// >=		MAYOR O IGUAL
	// <=		MENOR O IGUAL
	// <>		DISTINTO DE
	// ===	EXACTAMENTE IGUAL
	// !		NO LOGICO (INVERSION DE CONDICION)
	// and	Y LOGICO
	// or		O LOGICO
	//
		
	// OPERADORES ARITMETICOS
	// +		SUMAR
	// -		RESTA
	// *		MULTIPLICACION
	//	/		DIVISION (COCIENTE)
	// %		DIVISION (RESTO)
	// ()		PRECEDENCIA EN EL ORDEN DE LAS OPERACIONES
	// ++		OPERADOR DE INCREMENTO
	// --		OPERADOR DE DECREMENTO
	
	X=5;
	Y="5";
	
	if(X===Y){
		
	}
	
	if(Y == NaN){
		
	}
	//X+Y "55"
}